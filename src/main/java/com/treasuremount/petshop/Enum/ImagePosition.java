/*
package com.treasuremount.petshop.Enum;

public enum ImagePosition {
    FRONT,
    SIDE1,
    SIDE2,
    ANGLEDVIEW,
    BACK;

    // Utility method to validate position from a string
    public static ImagePosition fromString(String position) {
        for (ImagePosition p : ImagePosition.values()) {
            if (p.name().equalsIgnoreCase(position)) {
                return p;
            }
        }
        throw new IllegalArgumentException("Invalid position: " + position + ". Valid positions are: FRONT, SIDE1, SIDE2, ANGLEDVIEW.");
    }
}*/
